# 기여 안내

버그 수정, 새 레이아웃 프로필, 테스트, 문서 개선을 환영합니다.

## 원칙

- 실제 시험 문제·해설·문항 이미지를 커밋하지 않습니다.
- 재현 자료는 직접 만든 합성 이미지나 최소한의 좌표 JSON으로 만듭니다.
- PDF 자동 제안을 승인 결과처럼 취급하지 않습니다.
- 새 형식 지원은 문제지와 답지 프로필을 따로 검증합니다.
- 문항 번호를 바꾸는 변경은 문제, 정답표, 해설의 동기화를 테스트합니다.
- 새 용지 프로필은 실제 MediaBox와 전 페이지 렌더링 검사를 추가합니다.

## 개발 환경

```powershell
py -3.12 -m venv .venv
.\.venv\Scripts\pip.exe install -e .
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

Pull Request에는 문제 상황, 바뀐 동작, 테스트 결과, 새 합성 fixture의 생성 방법을
적어 주세요. 실제 자료에서만 재현되는 문제라면 원본을 올리지 말고 페이지 크기,
회전, 단 수, 번호 패턴, 실패 단계만 익명화해 설명해 주세요.

